<%= render_cell(
  :page_title,
  :show,
  data:
  {
    name: "#{t('model.list', model: Project.model_name.human)}",
    toolbar:
    {
      right:
      [
        render_cell(
          :button,
          :show,
          data:
          {
            name: fa_icon('plus-sign', text: t('model.create', :model => Project.model_name.human)),
            url: new_project_path,
            options:
            {
              class: 'btn btn-primary'
            }
          }
        )
      ]
    }
  }
)
%>

<div class="container-fluid">
  <div class="row-fluid">
    <div class="span6">
      <div class="btn-toolbar">

        <div class="btn-group">
          <%= link_to t('activerecord.attributes.project.show.all'), projects_path(show: :all), class: "btn" %>
          <%= link_to t('activerecord.attributes.project.show.top'), projects_path, class: "btn" %>
        </div>

      </div>
    </div>
    <div class="span6">

    </div>
  </div>
</div>

<%= render partial: 'projects_grid' %>
