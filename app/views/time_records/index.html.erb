<%= render_cell(
  :page_title,
  :show,
  data:
  {
    name: "#{t('model.list', model: TimeRecord.model_name.human)}",
    toolbar:
    {
      right:
      [
        render_cell(
          :button,
          :show,
          data:
          {
            name: fa_icon('plus-sign', text: t('model.create', :model => TimeRecord.model_name.human)),
            url: new_time_record_path,
            options:
            {
              class: 'btn btn-primary'
            }
          }
        )
      ]
    }
  }
)
%>

<%= render partial: 'time_records_grid' %>